// Напишите программу, которая на вход принимает позиции элемента 
// в двумерном массиве, и возвращает значение этого элемента или 
// же указание, что такого элемента нет.

// Например, задан массив:
// 1 4 7 2
// 5 9 2 3
// 8 4 2 4
// 17 -> такого числа в массиве нет

int[,] array = {
            { 1, 4, 7, 2 },
            { 5, 9, 2, 3 },
            { 8, 4, 2, 4 }
        };

        Console.WriteLine("Введите координаты элемента (строка столбец):");
        string input = Console.ReadLine();
        string[] position = input.Split(' ');

        int row, column;
        if (position.Length == 2 && int.TryParse(position[0], out row) && int.TryParse(position[1], out column))
        {
            if (row >= 0 && row < array.GetLength(0) && column >= 0 && column < array.GetLength(1))
            {
                int value = array[row, column];
                Console.WriteLine("Значение элемента: " + value);
            }
            else
            {
                Console.WriteLine("Указанные координаты выходят за границу массива.");
            }
        }
        else
        {
            Console.WriteLine("такого элемента нет.");
        }

        Console.ReadLine();

// Программа сначала объявляет двумерный массив array и инициализирует 
// его значениями. Затем она запрашивает у пользователя позицию 
// элемента в массиве. Введенная строка разбивается на два 
// числа - строку и столбец. После этого проверяется, что введенные 
// координаты находятся в допустимом диапазоне, и если да, 
// то выводится значение элемента из массива. Если же координаты 
// выходят за границу массива или ввод был некорректным,         